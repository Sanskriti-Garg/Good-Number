#include <stdio.h>
#include <math.h>
int main()
{
    int num,d,x,k;
    k=0;
    printf("Enter a Number :\n");
    scanf("%d",&num);
    x=num;
    while(x>0)
    {
        d=x%10;
        if(d==0)
        {
            k++;
        }
        x=x/10;
    }
    if(k>0)
    {
        printf("Not a Good Number");
    }
    else
    {
        printf("Good Number");
    }
     return 0;
}

